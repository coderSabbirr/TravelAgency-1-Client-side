import { Alert } from '@mui/material';
import React from 'react';

const Payment = () => {
    return (
        <div>
            <Alert variant='warning'>Comming soon</Alert>
        </div>
    );
};

export default Payment;